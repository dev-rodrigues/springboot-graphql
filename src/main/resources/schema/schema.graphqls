scalar LocalDate

type Query {
    promocoes(dataFilter: LocalDate): [Promotion]
    produtos(descricaoFilter: String): [Product]
}

type Promotion {
    descricao: String
    desconto: Float
    dataInicial: LocalDate
    dataFinal: LocalDate
}

type Product {
    id: Int
    descricao: String
    valor: Float
}